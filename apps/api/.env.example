# 数据库（由 RIC_* 环境变量拼接）
DATABASE_URL="postgresql://${RIC_PG_USER}:${RIC_PG_PASSWORD}@${RIC_PG_IP}:${RIC_PG_PORT}/cms?schema=public"

# Redis（由 RIC_* 环境变量拼接）
REDIS_URL="redis://:${RIC_REDIS_PASSWORD}@${RIC_REDIS_IP}:${RIC_REDIS_PORT}"

# Elasticsearch（由 RIC_* 环境变量拼接）
ES_HOST="${RIC_ES_IP}"
ES_PORT="${RIC_ES_PORT}"
ES_USER="${RIC_ES_USER}"
ES_PASSWORD="${RIC_ES_PASSWORD}"

# JWT (生产环境密钥)
JWT_SECRET="9ef12e4f37ed30534c3a1746168da66f6c3300678fe5b43d4d6f8eee7e123d73"
JWT_EXPIRES_IN="7d"

# 加密密钥
ENCRYPTION_KEY="bb148d4e8ba3fdc66b72db81cbab57bc"

# App
NODE_ENV="development"
PORT="3003"
FRONTEND_URL="http://localhost:3001"
FRONTEND_URLS="http://localhost:3001,http://localhost:3002"

# 文件上传
UPLOAD_DIR="./uploads"
MAX_FILE_SIZE="10485760"
